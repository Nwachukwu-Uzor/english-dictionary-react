import React, { useState } from "react";
import { Header, Container } from "../components";

export const Main: React.FC = () => {
  return (
    <>
      <Container>
        <Header />
      </Container>
    </>
  );
};
